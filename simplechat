#!/bin/bash

# Get the directory where the script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"

# Run the Python CLI script with all arguments passed through
python3 "$SCRIPT_DIR/src/cli.py" "$@"
